#!/usr/bin/env bash
# Codespace 에 접속할 때마다 터미널에 안내를 크게 출력한다 (devcontainer postAttachCommand).
cat <<'EOF'

==========================================================================
  네이버 블로그 자동화 대시보드

  잠시 뒤 이 편집기 안에 대시보드 창이 자동으로 열립니다.
  안 열리면: 아래 "포트(PORTS)" 탭 → 8787 줄 → 지구본 아이콘(브라우저에서 열기)

  이 터미널과 파일 화면은 건드리지 않아도 됩니다.
==========================================================================

EOF
